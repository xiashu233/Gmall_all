<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.manage.mapper.PmsProductInfoMapper">

    <select id="selectSkuSaleAttrValeBySpu" resultMap="SkuSaleAttrValeBySpuMap">
        select
            si.id as si_id,ssav.id as ssav_id,si.*,ssav.*
        from
            pms_sku_info si,pms_sku_sale_attr_value ssav
        where
            si.product_id = #{productId}
        and si.id = ssav.sku_id
    </select>
    <resultMap id="SkuSaleAttrValeBySpuMap" type="com.atguigu.gmall.bean.PmsSkuInfo" autoMapping="true">
        <result column="si_id" property="id" />
        <collection property="skuSaleAttrValueList" ofType="com.atguigu.gmall.bean.PmsSkuSaleAttrValue" autoMapping="true">
            <result property="id" column="ssav_id" />
        </collection>
    </resultMap>
</mapper>